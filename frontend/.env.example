VITE_API_URL=http://localhost:8080

# Social sign-in (buttons hidden when unset). Use VITE_* here — NOT GITHUB_CLIENT_ID.
# Copy the same GitHub Client ID as repo-root .env; keep GITHUB_CLIENT_SECRET only on the backend.
VITE_GOOGLE_CLIENT_ID=
VITE_GITHUB_CLIENT_ID=
