# Used when building for production (npm run build or Docker).
# Leave VITE_API_URL empty when nginx proxies /api on the same host (docker compose web service).

VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=
VITE_GITHUB_CLIENT_ID=
